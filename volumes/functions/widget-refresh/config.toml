# Enable CORS and JWT verification for widget-refresh function
[functions.widget-refresh]
cors = true
verify_jwt = true
allowed_origins = ["*"]
allowed_headers = ["Authorization", "Content-Type", "apikey", "x-client-info"]
allowed_methods = ["GET", "POST", "OPTIONS"]
