# Enable CORS and JWT verification for dashboard-manager function
[functions.dashboard-manager]
cors = true
verify_jwt = true
allowed_origins = ["*"]
allowed_headers = ["Authorization", "Content-Type", "apikey", "x-client-info"]
allowed_methods = ["GET", "POST", "PATCH", "DELETE", "OPTIONS"]
